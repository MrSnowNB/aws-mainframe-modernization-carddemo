skill_id: infra.patch
version: "1.1.0"
description: "Autonomous patching of modernization toolchain based on SIL tickets"
entrypoint: "atoms/infra_patch_worker.py"
requires: ["--pattern", "--apply"]   # --apply must be explicitly passed by inbox_watcher
default_mode: dry_run
safety:
  - backup_before_patch: true
  - py_compile_validation: true
  - automatic_rollback: true
  - human_gate_default: true
